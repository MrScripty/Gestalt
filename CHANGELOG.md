# Changelog

All notable changes to this project will be documented in this file.

The format is based on Keep a Changelog, and this project follows Semantic Versioning.

## [Unreleased]

### Added
- Native installer release pipeline producing `.deb`, `.msi`, and `.dmg` artifacts.
- Dioxus bundle metadata configuration for release packaging.

### Changed
- `launcher.sh` now supports full developer lifecycle actions (`--build`, `--build-release`, `--run-release`).

